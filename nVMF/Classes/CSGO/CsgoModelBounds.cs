using Narser.Two.Parser.Syntax.VMF.Nodes;

namespace Narser.Two.Classes.CSGO
{
    public sealed class CsgoModelBounds
    {
        private readonly CompoundNode _root;

        public CsgoModelBounds(CompoundNode root)
        {
            _root = root;
        }
    }
}